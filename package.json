{
  "name": "vis-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node prebuild.js && vite build && node postbuild.js",
    "lint": "oxlint",
    "preview": "vite preview",
    "deploy": "vite build && cd dist && git init && git checkout -b gh-pages && git add . && git commit -m \"Deploy to GitHub Pages\" && git push -f https://github.com/VISHAL-63449/portfolio.git gh-pages"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-icons": "^5.7.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "puppeteer": "^25.10.0",
    "vite": "^8.1.1"
  }
}
